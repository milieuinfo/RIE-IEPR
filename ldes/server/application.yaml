springdoc:
  swagger-ui:
    path: /v1/swagger
ldes-server:
  host-name: "http://ldes-server:8080"
management:
  tracing:
    enabled:
      false
spring:
  datasource:
    url: 'jdbc:postgresql://ldes-postgres:5432/ldesserver'
    username: admin
    password: admin

